<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include("../partials/head.ejs") %>

        <link rel="stylesheet" href="static/css/index.css">
    </head>
    <body>
        <%- include("../partials/nav.ejs") %>

        <main role="main">
            <h1 class="mt-5 mb-3" style="text-align: center">Photo Gallery</h1>
            <div class="d-flex flex-wrap align-items-center justify-content-center m-3">
                <% for (var image of images) { %>
                <img src="<%= image %>" alt="<%= image %>" class="flex-fill m-3" style="max-width: 500px; max-height: 500px">
                <% } %>
            </div>
        </main>
        
        <%- include("../partials/footer.ejs") %>
    </body>
</html>
